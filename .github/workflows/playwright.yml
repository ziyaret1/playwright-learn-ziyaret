
# name: Playwright Tests

# on:
#   push:
#     branches: ["*"] 
#   pull_request:
#     branches: [main] 

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Chromium only
#         run: npx playwright install chromium

#       - name: Run Playwright Tests with HTML & Allure reporters
#         run: npx playwright test --reporter=html,allure-playwright --workers=4
#         continue-on-error: true

#       - name: Install Allure CLI
#         run: npm install -g allure-commandline --save-dev

#       - name: Generate Allure Report
#         if: always()
#         run: allure generate ./allure-results --clean -o ./allure-report

#       - name: Upload Playwright HTML Report as Artifact
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: playwright-html-report
#           path: playwright-report

#       - name: Upload Allure Report as Artifact
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: allure-report
#           path: allure-report

#   deploy:
#     needs: test
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#     steps:
#       - name: Download Allure Report Artifact
#         uses: actions/download-artifact@v4
#         with:
#           name: allure-report
#           path: ./allure-report

#       - name: Deploy Allure Report to GitHub Pages 🚀
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./allure-report


#! check playwright reports to
name: Playwright Tests

on:
  push:
    branches: ["*"]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium only
        run: npx playwright install chromium

      - name: Run Playwright Tests with HTML & Allure reporters
        run: npx playwright test --reporter=html,allure-playwright --workers=4
        continue-on-error: true

      - name: Install Allure CLI
        run: npm install -g allure-commandline --save-dev

      - name: Generate Allure Report
        if: always()
        run: allure generate ./allure-results --clean -o ./allure-report

      - name: Upload Playwright HTML Report as Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report

      - name: Upload Allure Report as Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report

  deploy:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Allure Report Artifact
        uses: actions/download-artifact@v4
        with:
          name: allure-report
          path: ./site/allure-report

      - name: Download Playwright HTML Report Artifact
        uses: actions/download-artifact@v4
        with:
          name: playwright-html-report
          path: ./site/playwright-report

      - name: List site contents (debug)
        run: ls -R ./site

      - name: Create index.html for reports
        run: |
          echo '<!DOCTYPE html>' > ./site/index.html
          echo '<html lang="en">' >> ./site/index.html
          echo '<head><meta charset="UTF-8"><title>Test Reports</title>' >> ./site/index.html
          echo '<style>' >> ./site/index.html
          echo 'body { font-family: Arial, sans-serif; background: #f9f9f9; margin: 0; padding: 40px; }' >> ./site/index.html
          echo 'h1 { color: #333; text-align: center; margin-bottom: 30px; }' >> ./site/index.html
          echo 'ul { display: flex; justify-content: center; gap: 20px; list-style: none; padding: 0; }' >> ./site/index.html
          echo 'li { }' >> ./site/index.html
          echo 'a { display: block; padding: 15px 25px; background: #007bff; color: white; text-decoration: none; border-radius: 8px; font-weight: bold; transition: background 0.3s ease; }' >> ./site/index.html
          echo 'a:hover { background: #0056b3; }' >> ./site/index.html
          echo '</style></head><body>' >> ./site/index.html
          echo '<h1>📊 Test Reports</h1>' >> ./site/index.html
          echo '<ul>' >> ./site/index.html
          echo '<li><a href="allure-report/index.html">Allure Report</a></li>' >> ./site/index.html
          echo '<li><a href="playwright-report/index.html">Playwright HTML Report</a></li>' >> ./site/index.html
          echo '</ul>' >> ./site/index.html
          echo '</body></html>' >> ./site/index.html

      - name: Deploy Reports to GitHub Pages 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site

